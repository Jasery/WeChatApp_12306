/* pages/city/city.wxss */
/*
超级大坑，千万不要用自带的ide了，巨坑，
重命名页面后后，直接报错，修改app.json后重新帮我生成文件，
以前的东西就这么没了*/

.area {
    margin:20rpx 0;
    background-color: #fff;
}
.area-item {
    border-bottom:1rpx solid #F4F4F4;
    padding:20rpx 40rpx;
    display:flex;
}
.area-name {
    font-weight: 600;
    width: 100%;
}
.arrows {
    float: right;
    display: inline-block;
}
.arrows::after {
    content:">";
    color:#ccc;
}